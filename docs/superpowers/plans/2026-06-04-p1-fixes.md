# P1 修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复审阅发现的三项 P1 问题——补齐 3 个 Agent 的 `evaluate()`、建立新栈 specs/prompt 体系、提取 `routers.py`。

**Architecture:** 共 7 个 Task 分三阶段：① evaluate() 实装（Task 1-3）让 ComponentBench 可运行；② routers.py 提取（Task 4）对齐 spec §7 目录结构；③ specs/prompt 体系（Task 5-7）将硬编码 prompt 迁移到 `.prompt.md` 文件 + 轻量 SpecLoader。所有改动严格 TDD、不破坏现有 439 通过测试。

**Tech Stack:** Python 3.12 / pytest / StrEnum / LLMService（`invoke_json`）/ FakeLLM

---

## 文件归属

| Task | 创建 | 修改 | 测试 |
|------|------|------|------|
| 1 | — | `app/agents/critic.py` | `tests/unit/agents/test_critic.py` |
| 2 | — | `app/agents/tutor.py` | `tests/unit/agents/test_tutor.py` |
| 3 | — | `app/agents/conductor.py` | `tests/unit/agents/test_conductor.py` |
| 4 | `app/orchestration/routers.py` | `app/orchestration/graph.py` | `tests/unit/orchestration/test_routers.py` |
| 5 | `app/specs/_root.prompt.md` · `app/specs/agents/*.prompt.md` · `app/specs/event_map.yaml` | — | — |
| 6 | `app/specs/__init__.py` · `app/specs/loader.py` | — | `tests/unit/specs/test_loader.py` |
| 7 | — | `app/agents/{tutor,critic,conductor}.py` | 既有 Agent 测试（回归验证） |

---

### Task 1: Critic.evaluate() 实装

**Files:**
- Modify: `app/agents/critic.py:102-103`
- Test: `tests/unit/agents/test_critic.py`

黄金用例契约（`app/eval/fixtures/golden_cases.py`）：
```
input:    {"user_text": str, "topic": str}
expected: {"mastery_level": "partial"}   # 字符串比较
```

实现策略：构造 `UserMessage` 事件，调用自身 `_assess_user_message`，从产出事件中提取指标。

- [ ] **Step 1: 写失败测试**

在 `tests/unit/agents/test_critic.py` 末尾追加：

```python
def test_critic_evaluate_returns_mastery_level(mock_llm_invoke_json):
    mock_llm_invoke_json({"critic_assess": {
        "mastery_level": "partial",
        "mastery_score": 55,
        "rationale": "基本概念正确但不完整",
    }})
    critic = CriticAgent()
    result = critic.evaluate({"user_text": "RAG是检索增强生成", "topic": "RAG"})
    assert result["mastery_level"] == "partial"
    assert "confusion_detected" in result
    assert "contradiction_detected" in result


def test_critic_evaluate_detects_confusion(mock_llm_invoke_json):
    mock_llm_invoke_json({"critic_assess": {
        "mastery_level": "weak",
        "mastery_score": 30,
        "rationale": "混淆概念",
        "confusion": {"concept_a": "retrieval", "concept_b": "fine-tuning"},
    }})
    critic = CriticAgent()
    result = critic.evaluate({"user_text": "RAG就是微调", "topic": "RAG"})
    assert result["mastery_level"] == "weak"
    assert result["confusion_detected"] is True
```

- [ ] **Step 2: 运行测试验证失败**

Run: `.venv/bin/python -m pytest tests/unit/agents/test_critic.py::test_critic_evaluate_returns_mastery_level tests/unit/agents/test_critic.py::test_critic_evaluate_detects_confusion -v < /dev/null`
Expected: FAIL (`NotImplementedError`)

- [ ] **Step 3: 实现 Critic.evaluate()**

替换 `app/agents/critic.py` 中 `evaluate` 方法（约第 102-103 行）：

```python
def evaluate(self, test_case: dict) -> dict:
    """部件级评估（§5.2）：对给定用户文本做语义评估，返回标准化指标。

    test_case: {"user_text": str, "topic": str}
    返回: {"mastery_level": str, "mastery_score": int|None,
           "confusion_detected": bool, "contradiction_detected": bool,
           "low_confidence_detected": bool}
    """
    ws = WorkspaceState(session_id="__eval__", user_id="__eval__",
                        current_topic=test_case.get("topic", ""))
    fake_event = Event(
        type=EventType.USER_MESSAGE, source=EventSource.USER,
        session_id="__eval__",
        payload={"text": test_case.get("user_text", "")})
    produced = self._assess_user_message(fake_event, ws)
    result: dict = {
        "mastery_level": None,
        "mastery_score": None,
        "confusion_detected": False,
        "contradiction_detected": False,
        "low_confidence_detected": False,
    }
    for ev in produced:
        if ev.type == EventType.MASTERY_ASSESSED:
            result["mastery_level"] = ev.payload.get("level")
            result["mastery_score"] = ev.payload.get("score")
        elif ev.type == EventType.CONFUSION_DETECTED:
            result["confusion_detected"] = True
        elif ev.type == EventType.CONTRADICTION_DETECTED:
            result["contradiction_detected"] = True
        elif ev.type == EventType.LOW_CONFIDENCE_DETECTED:
            result["low_confidence_detected"] = True
    return result
```

- [ ] **Step 4: 运行测试验证通过**

Run: `.venv/bin/python -m pytest tests/unit/agents/test_critic.py -v < /dev/null`
Expected: ALL PASS

- [ ] **Step 5: 提交**

```bash
git add app/agents/critic.py tests/unit/agents/test_critic.py
git commit -m "feat(eval): implement Critic.evaluate() for ComponentBench (§5.2)"
```

---

### Task 2: Tutor.evaluate() 实装

**Files:**
- Modify: `app/agents/tutor.py:96-97`
- Test: `tests/unit/agents/test_tutor.py`

黄金用例契约：
```
input:    {"topic": str, "action": str}
expected: {"explanation_completeness": 0.7}   # 数值 >= 阈值
```

实现策略：构造 `ActionRequested` 事件调用 `handle()`，从产出内容中计算启发式指标。`explanation_completeness` 用「产出内容中与 topic 相关关键词覆盖度」衡量；无 `golden_response` 时以内容非空长度作为弱代理。

- [ ] **Step 1: 写失败测试**

在 `tests/unit/agents/test_tutor.py` 末尾追加：

```python
def test_tutor_evaluate_returns_completeness(mock_llm_invoke_json):
    mock_llm_invoke_json({"tutor_explain": {
        "content": "RAG 是 Retrieval-Augmented Generation，即检索增强生成。"
                   "它通过检索外部知识库来增强 LLM 的回答质量。",
    }})
    tutor = TutorAgent()
    result = tutor.evaluate({"topic": "RAG", "action": "tutor_explain"})
    assert "explanation_completeness" in result
    assert result["explanation_completeness"] > 0
    assert "response_length" in result
    assert result["response_length"] > 0


def test_tutor_evaluate_with_golden_response(mock_llm_invoke_json):
    mock_llm_invoke_json({"tutor_explain": {"content": "RAG 是检索增强生成技术"}})
    tutor = TutorAgent()
    result = tutor.evaluate({
        "topic": "RAG", "action": "tutor_explain",
        "golden_response": "RAG 是 Retrieval Augmented Generation，检索增强生成",
    })
    assert "explanation_completeness" in result
    assert 0 <= result["explanation_completeness"] <= 1.0
```

- [ ] **Step 2: 运行测试验证失败**

Run: `.venv/bin/python -m pytest tests/unit/agents/test_tutor.py::test_tutor_evaluate_returns_completeness tests/unit/agents/test_tutor.py::test_tutor_evaluate_with_golden_response -v < /dev/null`
Expected: FAIL (`NotImplementedError`)

- [ ] **Step 3: 实现 Tutor.evaluate()**

替换 `app/agents/tutor.py` 中 `evaluate` 方法（约第 96-97 行）：

```python
def evaluate(self, test_case: dict) -> dict:
    """部件级评估（§5.2）：生成教学内容并计算质量指标。

    test_case: {"topic": str, "action": str, "golden_response"?: str}
    返回: {"explanation_completeness": float, "response_length": int}
    """
    from collections import Counter

    topic = test_case.get("topic", "")
    action = test_case.get("action", "tutor_explain")
    golden = test_case.get("golden_response", "")

    ws = WorkspaceState(session_id="__eval__", user_id="__eval__",
                        current_topic=topic)
    trigger = Event(
        type=EventType.ACTION_REQUESTED, source=EventSource.ORCHESTRATOR,
        session_id="__eval__",
        payload={"action": action, "target": str(EventSource.TUTOR)})
    produced = self.handle(trigger, ws)

    content = ""
    for ev in produced:
        content = ev.payload.get("content", "")
        if content:
            break

    response_length = len(content)
    if golden and content:
        gc = Counter(golden)
        cc = Counter(content)
        intersection = sum((gc & cc).values())
        union = sum((gc | cc).values())
        completeness = intersection / union if union else 0.0
    elif content:
        completeness = min(response_length / 50, 1.0)
    else:
        completeness = 0.0

    return {
        "explanation_completeness": round(completeness, 4),
        "response_length": response_length,
    }
```

- [ ] **Step 4: 运行测试验证通过**

Run: `.venv/bin/python -m pytest tests/unit/agents/test_tutor.py -v < /dev/null`
Expected: ALL PASS

- [ ] **Step 5: 提交**

```bash
git add app/agents/tutor.py tests/unit/agents/test_tutor.py
git commit -m "feat(eval): implement Tutor.evaluate() for ComponentBench (§5.2)"
```

---

### Task 3: Conductor.evaluate() 实装

**Files:**
- Modify: `app/agents/conductor.py:48-49`
- Test: `tests/unit/agents/test_conductor.py`

黄金用例契约：
```
input:    {"observations": list[dict], "current_mode": str}
expected: {"action": "request_observation", "observation_enough": False}
```

实现策略：构造 `ConductorRequested` 事件调用 `handle()`，提取 `ConductorDecided` 的 payload 字段。

- [ ] **Step 1: 写失败测试**

在 `tests/unit/agents/test_conductor.py` 末尾追加：

```python
def test_conductor_evaluate_empty_observations(mock_llm_invoke_json):
    mock_llm_invoke_json({"conductor_decide": {
        "action": "request_observation",
        "reason": "观察不足",
        "observation_enough": False,
        "target": "critic",
    }})
    conductor = ConductorAgent()
    result = conductor.evaluate({
        "observations": [],
        "current_mode": "Socratic",
    })
    assert result["action"] == "request_observation"
    assert result["observation_enough"] is False


def test_conductor_evaluate_with_observations(mock_llm_invoke_json):
    mock_llm_invoke_json({"conductor_decide": {
        "action": "tutor_offer_analogy",
        "reason": "混淆需要类比",
        "observation_enough": True,
    }})
    conductor = ConductorAgent()
    result = conductor.evaluate({
        "observations": [{"type": "ConfusionDetected"}],
        "current_mode": "Socratic",
    })
    assert result["action"] == "tutor_offer_analogy"
    assert result["observation_enough"] is True
```

- [ ] **Step 2: 运行测试验证失败**

Run: `.venv/bin/python -m pytest tests/unit/agents/test_conductor.py::test_conductor_evaluate_empty_observations tests/unit/agents/test_conductor.py::test_conductor_evaluate_with_observations -v < /dev/null`
Expected: FAIL (`NotImplementedError`)

- [ ] **Step 3: 实现 Conductor.evaluate()**

替换 `app/agents/conductor.py` 中 `evaluate` 方法（约第 48-49 行）：

```python
def evaluate(self, test_case: dict) -> dict:
    """部件级评估（§5.2）：给定观察集做决策，返回决策结果指标。

    test_case: {"observations": list[dict], "current_mode": str}
    返回: {"action": str, "observation_enough": bool, "reason": str}
    """
    ws = WorkspaceState(
        session_id="__eval__", user_id="__eval__",
        current_mode=test_case.get("current_mode", "Socratic"))
    trigger = Event(
        type=EventType.CONDUCTOR_REQUESTED, source=EventSource.ORCHESTRATOR,
        session_id="__eval__",
        payload={
            "observations": test_case.get("observations", []),
            "reason": "evaluate",
        })
    produced = self.handle(trigger, ws)
    if not produced:
        return {"action": "", "observation_enough": False, "reason": "no output"}
    ev = produced[0]
    return {
        "action": ev.payload.get("action", ""),
        "observation_enough": ev.payload.get("observation_enough", False),
        "reason": ev.payload.get("reason", ""),
    }
```

注意：需要在文件顶部补充 `WorkspaceState` 导入（当前 conductor.py 已有此 import）。

- [ ] **Step 4: 运行测试验证通过**

Run: `.venv/bin/python -m pytest tests/unit/agents/test_conductor.py -v < /dev/null`
Expected: ALL PASS

- [ ] **Step 5: 提交**

```bash
git add app/agents/conductor.py tests/unit/agents/test_conductor.py
git commit -m "feat(eval): implement Conductor.evaluate() for ComponentBench (§5.2)"
```

---

### Task 4: 提取 routers.py（对齐 spec §7）

**Files:**
- Create: `app/orchestration/routers.py`
- Modify: `app/orchestration/graph.py:83-84`
- Test: `tests/unit/orchestration/test_routers.py`

当前条件边逻辑内联在 `graph.py` 的 `_route_decision` 函数中。提取为独立模块，保持功能等价。

- [ ] **Step 1: 写失败测试**

创建 `tests/unit/orchestration/test_routers.py`：

```python
from app.orchestration.routers import route_decision


def test_route_decision_default_enters_collab_loop():
    assert route_decision({"enter_loop": True}) == "collab_loop"


def test_route_decision_enter_loop_false_goes_wrap_up():
    assert route_decision({"enter_loop": False}) == "wrap_up"


def test_route_decision_missing_field_defaults_to_collab_loop():
    assert route_decision({}) == "collab_loop"
```

- [ ] **Step 2: 运行测试验证失败**

Run: `.venv/bin/python -m pytest tests/unit/orchestration/test_routers.py -v < /dev/null`
Expected: FAIL (`ModuleNotFoundError: No module named 'app.orchestration.routers'`)

- [ ] **Step 3: 创建 routers.py**

创建 `app/orchestration/routers.py`：

```python
"""主图条件边（§3.5.4 / spec §7）。"""


def route_decision(state: dict) -> str:
    """route 节点的下游判定：是否进入协作环。"""
    return "collab_loop" if state.get("enter_loop", True) else "wrap_up"
```

- [ ] **Step 4: 运行测试验证通过**

Run: `.venv/bin/python -m pytest tests/unit/orchestration/test_routers.py -v < /dev/null`
Expected: ALL PASS

- [ ] **Step 5: 改造 graph.py 引用 routers**

在 `app/orchestration/graph.py` 中：

删除内联函数 `_route_decision`（约第 83-84 行）：
```python
# 删除这两行
def _route_decision(state: MainState) -> str:
    return "collab_loop" if state.get("enter_loop", True) else "wrap_up"
```

在文件顶部 import 区域追加：
```python
from app.orchestration.routers import route_decision
```

将 `build_main_graph` 中的 `_route_decision` 引用替换为 `route_decision`：
```python
g.add_conditional_edges("route", route_decision,
                        {"collab_loop": "collab_loop", "wrap_up": "wrap_up"})
```

- [ ] **Step 6: 回归验证**

Run: `.venv/bin/python -m pytest tests/unit/orchestration/ -v < /dev/null`
Expected: ALL PASS（含已有 `test_graph.py` 回归）

- [ ] **Step 7: 提交**

```bash
git add app/orchestration/routers.py app/orchestration/graph.py tests/unit/orchestration/test_routers.py
git commit -m "refactor: extract routers.py from graph.py (align spec §7)"
```

---

### Task 5: 创建 .prompt.md 规范文件

**Files:**
- Create: `app/specs/__init__.py`
- Create: `app/specs/_root.prompt.md`
- Create: `app/specs/agents/tutor.prompt.md`
- Create: `app/specs/agents/critic.prompt.md`
- Create: `app/specs/agents/conductor.prompt.md`
- Create: `app/specs/agents/retriever.prompt.md`
- Create: `app/specs/agents/curator.prompt.md`
- Create: `app/specs/event_map.yaml`

从现有代码中提取硬编码 prompt 内容到文件。以下是 7 处调用点的提取：

| 文件 | intent | 当前硬编码 system_prompt |
|------|--------|--------------------------|
| tutor.py:49 | tutor_request_recap | "你是融合式教学的 Tutor，切入费曼模式让用户复述。" |
| tutor.py:58 | tutor_offer_analogy | "你是融合式教学的 Tutor，给出类比破除概念混淆。" |
| tutor.py:73 | tutor_ask / tutor_probe_prereq | "你是融合式教学的 Tutor，在 Socratic 模式下抛出引导问题。" |
| tutor.py:86 | tutor_explain / re_explain / correct | "你是融合式教学的 Tutor，根据模式给出讲解。" |
| critic.py:42 | critic_assess | "你是融合式教学的 Critic，对用户回答做语义评估。输出 JSON…" |
| critic.py:85 | critic_rag_quality | "你是融合式教学的 Critic，评估证据对当前教学是否相关、是否充分。输出 JSON…" |
| conductor.py:31 | conductor_decide | "你是融合式教学的 Conductor。只能基于已有观察事件做路由决策…" |

- [ ] **Step 1: 创建 `app/specs/__init__.py`**

```python
```

（空文件，使 `app/specs` 成为 Python 包）

- [ ] **Step 2: 创建 `app/specs/_root.prompt.md`**

```markdown
# 全局规则

你是 StudyAgent 学习辅助系统的一部分。所有 Agent 遵守以下底线规则：

- 使用中文与用户交流
- 所有输出必须结构化为 JSON，除非明确要求自由文本
- 禁止编造事实；不确定时说明"不确定"
- 禁止越权：只做自己职能范围内的事
- 响应应简洁、精准、有教学针对性
```

- [ ] **Step 3: 创建 `app/specs/agents/tutor.prompt.md`**

```markdown
# Tutor — 教学主体

你是融合式教学系统的 Tutor，负责生成教学内容。

## 角色定义

- 执行讲解、提问、追问、发起复述请求、类比生成
- **绝不评判**用户回答的质量（复述质量、掌握度归 Critic）
- 根据 Orchestrator 下达的动作类型生成对应内容

## 动作指令

### tutor_ask / tutor_probe_prereq

在 Socratic 模式下抛出引导性问题，不直接给答案。
probe_prereq 时，针对前置知识点发轻量探测问题。
输出 JSON: {"content": "问题文本"}

### tutor_explain / tutor_re_explain / tutor_correct

根据模式给出讲解。explain 为首次讲解；re_explain 换角度重讲；correct 纠正矛盾。
输出 JSON: {"content": "讲解文本"}

### tutor_request_recap

切入费曼模式，让用户用自己的话复述所学内容。
输出 JSON: {"content": "复述请求文本"}

### tutor_offer_analogy

给出类比/比喻，帮助用户破除概念混淆。
输出 JSON: {"content": "类比文本", "analogy_target": "类比对象(可选)"}
```

- [ ] **Step 4: 创建 `app/specs/agents/critic.prompt.md`**

```markdown
# Critic — 文本语义评估

你是融合式教学系统的 Critic，只判文本语义层。

## 角色定义

- 评估掌握度、检测概念混淆、自相矛盾、回答置信度、RAG 语义质量
- **不读图谱、不判前置缺失、不做路由决策**

## 动作指令

### critic_assess

对用户回答做语义评估。
输出 JSON: {"mastery_level": "weak|partial|mastered", "mastery_score": 0-100,
"rationale": "判定理由",
"confusion": {"concept_a": "...", "concept_b": "..."} (可选),
"contradiction": {"description": "..."} (可选),
"low_confidence": true/false (可选)}

### critic_rag_quality

评估证据对当前教学是否相关、是否充分。仅在 purpose=teaching 时触发。
输出 JSON: {"score": 0-1, "relevance": 0-1, "sufficiency": 0-1, "rationale": "判定理由"}
```

- [ ] **Step 5: 创建 `app/specs/agents/conductor.prompt.md`**

```markdown
# Conductor — LLM 决策兜底

你是融合式教学系统的 Conductor，在规则引擎未覆盖时做路由决策。

## 角色定义

- 只能基于已有观察事件（Critic/Curator 产出）做路由决策
- **禁止自产语义/结构观察**
- 观察不足时，输出 action=request_observation + target=critic|curator

## 动作指令

### conductor_decide

基于观察集决定下一步动作。
若观察足够：输出 {"action": "动作名", "reason": "理由", "observation_enough": true}
若观察不足：输出 {"action": "request_observation", "target": "critic|curator", "reason": "缺XX评估", "observation_enough": false}
```

- [ ] **Step 6: 创建 `app/specs/agents/retriever.prompt.md`**

```markdown
# Retriever — 知识检索

只做机械层检索。不评判"证据够不够好"（语义质量归 Critic）。
本 Agent 当前不使用 LLM，此文件仅作角色声明留存。
```

- [ ] **Step 7: 创建 `app/specs/agents/curator.prompt.md`**

```markdown
# Curator — 用户画像与知识图谱

只判结构层：基于图谱前置关系 + 用户掌握度判"前置薄弱"。不判文本语义。
本 Agent 当前不使用 LLM（冷启动 LLM_INFER 建图待后续启用），此文件仅作角色声明留存。
```

- [ ] **Step 8: 创建 `app/specs/event_map.yaml`**

```yaml
# 事件→Agent 映射（取代旧 intent_map.yaml，§10）
# 格式：EventType → 订阅者 Agent → 产出的 EventType

subscriptions:
  ActionRequested:
    - agent: tutor
      filter: "payload.target == tutor"
      emits: [TutorAsked, TutorExplained, TutorRequestedRecap, TutorOfferedAnalogy]
    - agent: retriever
      filter: "payload.target == retriever"
      emits: [RetrievedEvidence, RetrievalFailed]

  UserMessage:
    - agent: critic
      emits: [MasteryAssessed, ConfusionDetected, ContradictionDetected, LowConfidenceDetected]

  RetrievedEvidence:
    - agent: critic
      filter: "payload.purpose == teaching"
      emits: [RAGQualityAssessed]

  MasteryAssessed:
    - agent: curator
      emits: [ProfileUpdated, GraphNodeStrengthened, GraphPrereqWeakDetected]

  TopicEntered:
    - agent: curator
      emits: [GraphPrereqWeakDetected]

  ConductorRequested:
    - agent: conductor
      emits: [ConductorDecided]
```

- [ ] **Step 9: 提交**

```bash
git add app/specs/
git commit -m "feat(specs): create new-stack prompt specs and event_map (§10)"
```

---

### Task 6: 实现 SpecLoader

**Files:**
- Create: `app/specs/loader.py`
- Test: `tests/unit/specs/test_loader.py`

轻量级加载器：读取 `_root.prompt.md` + `agents/<name>.prompt.md`，拼接为 system prompt，按 intent 提取子指令段落。

- [ ] **Step 1: 写失败测试**

创建 `tests/unit/specs/__init__.py`（空文件）和 `tests/unit/specs/test_loader.py`：

```python
from app.specs.loader import SpecLoader


def test_loader_compose_tutor_ask():
    loader = SpecLoader()
    prompt = loader.compose("tutor", "tutor_ask")
    assert "全局规则" in prompt
    assert "Tutor" in prompt
    assert "tutor_ask" in prompt or "Socratic" in prompt


def test_loader_compose_critic_assess():
    loader = SpecLoader()
    prompt = loader.compose("critic", "critic_assess")
    assert "Critic" in prompt
    assert "mastery_level" in prompt


def test_loader_compose_conductor_decide():
    loader = SpecLoader()
    prompt = loader.compose("conductor", "conductor_decide")
    assert "Conductor" in prompt
    assert "observation" in prompt


def test_loader_compose_unknown_agent_returns_root_only():
    loader = SpecLoader()
    prompt = loader.compose("nonexistent", "some_intent")
    assert "全局规则" in prompt


def test_loader_caches_file_reads():
    loader = SpecLoader()
    p1 = loader.compose("tutor", "tutor_ask")
    p2 = loader.compose("tutor", "tutor_explain")
    # 同一个 agent 文件不重复读取
    assert "Tutor" in p1
    assert "Tutor" in p2
```

- [ ] **Step 2: 运行测试验证失败**

Run: `.venv/bin/python -m pytest tests/unit/specs/test_loader.py -v < /dev/null`
Expected: FAIL (`ModuleNotFoundError`)

- [ ] **Step 3: 实现 SpecLoader**

创建 `app/specs/loader.py`：

```python
"""渐进式 Prompt 加载器（§10 / agent-root.md 要求）。

compose(agent, intent) → 拼接 _root + agents/<agent> 两层 prompt，
并在 agent prompt 中定位 intent 对应的子指令段落追加到末尾。
"""

import re
from functools import lru_cache
from pathlib import Path

_SPECS_DIR = Path(__file__).resolve().parent


@lru_cache(maxsize=32)
def _read_file(path: Path) -> str:
    if path.exists():
        return path.read_text(encoding="utf-8")
    return ""


class SpecLoader:
    def __init__(self, specs_dir: Path | None = None):
        self._dir = specs_dir or _SPECS_DIR

    def compose(self, agent: str, intent: str) -> str:
        root = _read_file(self._dir / "_root.prompt.md")
        agent_spec = _read_file(self._dir / "agents" / f"{agent}.prompt.md")
        parts = [root]
        if agent_spec:
            parts.append(agent_spec)
        intent_section = self._extract_section(agent_spec, intent)
        if intent_section:
            parts.append(f"\n## 当前任务: {intent}\n\n{intent_section}")
        return "\n\n---\n\n".join(p for p in parts if p.strip())

    @staticmethod
    def _extract_section(text: str, intent: str) -> str:
        if not text or not intent:
            return ""
        pattern = re.compile(
            rf"^###\s+{re.escape(intent)}\b.*?$",
            re.MULTILINE | re.IGNORECASE,
        )
        match = pattern.search(text)
        if not match:
            return ""
        start = match.end()
        next_heading = re.search(r"^###\s+", text[start:], re.MULTILINE)
        end = start + next_heading.start() if next_heading else len(text)
        return text[start:end].strip()
```

- [ ] **Step 4: 运行测试验证通过**

Run: `.venv/bin/python -m pytest tests/unit/specs/test_loader.py -v < /dev/null`
Expected: ALL PASS

- [ ] **Step 5: 提交**

```bash
git add app/specs/loader.py tests/unit/specs/__init__.py tests/unit/specs/test_loader.py
git commit -m "feat(specs): implement SpecLoader for progressive prompt loading (§10)"
```

---

### Task 7: 改造 Agent 使用 SpecLoader

**Files:**
- Modify: `app/agents/tutor.py`
- Modify: `app/agents/critic.py`
- Modify: `app/agents/conductor.py`
- Test: 既有测试回归验证

将 3 个 Agent 中的 7 处硬编码 prompt 替换为 `SpecLoader.compose()` 调用。Agent 构造函数新增可选 `spec_loader` 参数（默认创建实例），保持向后兼容。

- [ ] **Step 1: 改造 TutorAgent**

在 `app/agents/tutor.py` 中：

顶部追加 import：
```python
from app.specs.loader import SpecLoader
```

修改构造函数：
```python
def __init__(self, llm: LLMService | None = None,
             spec_loader: SpecLoader | None = None):
    self._llm = llm or LLMService()
    self._spec = spec_loader or SpecLoader()
```

替换 4 处硬编码 system_prompt 调用：

`_ask` 方法（约第 73 行）：
```python
result = self._llm.invoke_json(
    self._spec.compose("tutor", intent),
    f"主题：{ws.current_topic or ''}",
    session_id=ws.session_id, node="tutor", intent=intent,
)
```

`_explain` 方法（约第 86 行）：
```python
result = self._llm.invoke_json(
    self._spec.compose("tutor", intent),
    f"主题：{ws.current_topic or ''}\n模式：{mode}",
    session_id=ws.session_id, node="tutor", intent=intent,
)
```

`handle` 中 `TUTOR_REQUEST_RECAP` 分支（约第 48 行）：
```python
result = self._llm.invoke_json(
    self._spec.compose("tutor", "tutor_request_recap"),
    f"主题：{ws.current_topic or ''}",
    session_id=ws.session_id, node="tutor",
    intent="tutor_request_recap",
)
```

`handle` 中 `TUTOR_OFFER_ANALOGY` 分支（约第 58 行）：
```python
result = self._llm.invoke_json(
    self._spec.compose("tutor", "tutor_offer_analogy"),
    f"主题：{ws.current_topic or ''}",
    session_id=ws.session_id, node="tutor",
    intent="tutor_offer_analogy",
)
```

- [ ] **Step 2: 改造 CriticAgent**

在 `app/agents/critic.py` 中：

追加 import + 修改构造函数（同 Tutor 模式）：
```python
from app.specs.loader import SpecLoader

# 构造函数
def __init__(self, llm: LLMService | None = None,
             spec_loader: SpecLoader | None = None):
    self._llm = llm or LLMService()
    self._spec = spec_loader or SpecLoader()
```

替换 2 处：

`_assess_user_message`（约第 42 行）：
```python
result = self._llm.invoke_json(
    self._spec.compose("critic", "critic_assess"),
    f"主题：{ws.current_topic or ''}\n用户回答：{text}",
    session_id=ws.session_id, node="critic", intent="critic_assess",
)
```

`_assess_rag_quality`（约第 85 行）：
```python
result = self._llm.invoke_json(
    self._spec.compose("critic", "critic_rag_quality"),
    f"主题：{ws.current_topic or ''}\n证据条数：{len(chunks)}",
    session_id=ws.session_id, node="critic", intent="critic_rag_quality",
)
```

- [ ] **Step 3: 改造 ConductorAgent**

在 `app/agents/conductor.py` 中：

追加 import + 修改构造函数 + 替换 1 处：

```python
from app.specs.loader import SpecLoader

# 构造函数
def __init__(self, llm: LLMService | None = None,
             spec_loader: SpecLoader | None = None):
    self._llm = llm or LLMService()
    self._spec = spec_loader or SpecLoader()
```

`handle` 中（约第 31 行）：
```python
result = self._llm.invoke_json(
    self._spec.compose("conductor", "conductor_decide"),
    f"观察集：{observations}\n当前模式：{ws.current_mode}",
    session_id=ws.session_id, node="conductor",
    intent="conductor_decide",
)
```

- [ ] **Step 4: 回归验证**

Run: `.venv/bin/python -m pytest tests/unit/agents/ tests/unit/orchestration/ tests/eval/ -v < /dev/null`
Expected: ALL PASS（mock_llm_invoke_json 按 intent 匹配，不依赖 system_prompt 内容，故不受影响）

- [ ] **Step 5: 全量回归**

Run: `.venv/bin/python -m pytest tests/ -q < /dev/null`
Expected: 4 failed（预存 test_stores.py），其余全通过

- [ ] **Step 6: 提交**

```bash
git add app/agents/tutor.py app/agents/critic.py app/agents/conductor.py
git commit -m "refactor: wire agents to SpecLoader, remove hardcoded prompts (§10)"
```

---

## 验收标准

1. `ComponentBench` 可对全部 5 Agent 运行 `evaluate()`，无 `NotImplementedError`
2. `app/orchestration/routers.py` 存在且被 `graph.py` 引用
3. `app/specs/` 目录含 `_root.prompt.md` + 5 个 `agents/*.prompt.md` + `event_map.yaml`
4. 3 个 Agent 的 LLM 调用 system prompt 来自 `SpecLoader.compose()`
5. `pytest tests/ -q` 输出 ≥ 449 passed，4 failed（仅预存 test_stores.py）
